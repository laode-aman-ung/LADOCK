# LADOCK Desktop — Python dependencies
# Install with:  pip install -r requirements.txt
# Or (editable):  pip install -e .

PySide6>=6.5
numpy>=1.24
scipy>=1.10
pandas>=2.0

# Optional — 2D SMILES depiction (LID diagram style in interaction panel)
# rdkit>=2023.3

# Optional — embedded 3D viewer (required on distro packages that split Qt WebEngine)
# If the 3D viewer opens in your browser instead of the app, install this:
# PySide6-WebEngine>=6.5
