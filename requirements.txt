# LADOCK Desktop — Python dependencies
# Install with:  pip install -r requirements.txt
# Or (editable):  pip install -e .

PySide6>=6.5
numpy>=1.24
scipy>=1.10
pandas>=2.0

# Optional — SMILES rendering and ligand conversion
# rdkit>=2023.3
